<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TheTopNews" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/mysteryBlack</item>
        <item name="colorPrimaryVariant">@color/mysteryBlack</item>
        <item name="colorOnPrimary">@color/mysteryBlack</item>
        <!-- Secondary brand color. -->

        <item name="colorOnSecondary">@color/mysteryBlack</item>
        <item name="android:textColorPrimary">@color/lightWhite</item>
        <item name="android:background">@color/mysteryBlack</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TopNewsStyle.TextStyle">
        <item name="android:textColor">@color/lightWhite</item>
    </style>
</resources>